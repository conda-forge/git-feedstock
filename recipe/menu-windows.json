{
    "menu_name": "Anaconda${PY_VER} ${PLATFORM}",
    "menu_items":
    [
    {
        "name": "MSYS2 Bash",
        "script": "${PREFIX}/Library/usr/bin/bash.exe",
        "scriptarguments": ["--login", "-i", "--"],
        "workdir": "${PERSONALDIR}",
        "icon": "${MENU_DIR}/git-for-windows.ico"
    }
    ]
}
